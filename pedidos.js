function enviar_pedido(){
    var nome = document.getElementById("name_id");

    if (nome.value != ""){
        alert("Obrigado Sr(a) " + nome.value + "os seus dados foram salvos com sucesse!");

    }
    var fone = document.getElementById("tel_id");

    if (fone.value != "",git ){
        alert("erro");
    }


}

